# CLAUDE.md — SME Interview Web Instrument

This file is a handoff brief for a Claude Code session picking up this project cold. Read this whole file before touching any code — several design decisions here look arbitrary until you know why they exist, and a few of them are load-bearing (breaking them silently breaks the study's methodology, not just the code).

---

## What this project is

A single-page web app that runs a study for a master's thesis: *"Development of a Mobile-Native AI-Powered Code Review Application Using Serverless Architecture"* (Muhammad Syaheer Daniel, MMSD, UTeM FTMK). The participant (or a researcher, typing on their behalf during a live session) starts with these common stages:

**Research-title display (14 August 2026, local until deployed):** the public study header and browser-tab title now show the complete official thesis title, “Development of a Mobile-Native AI-Powered Code Review Application Using Serverless Architecture,” beneath the concise Glance study label.

**Route-neutral cover copy (14 August 2026, local until deployed):** the cover is shown before a person chooses SME or SUS-only. It now calls the work a “Usability and professional evaluation study,” explains that both routes include the demonstration, hands-on task, and SUS, and identifies the professional discussion/category exercise as SME-only.

1. **Consent** — participant reads the study information and explicitly agrees before an ID or any response is created. The accepted form version and first timestamp are recorded later against that ID.
2. **Study path selection** — browser code `0811` selects the invited full-SME route. Continuing without it selects the SUS-only route. This is researcher-supervised routing, not a login or security boundary.

The **full SME route** then follows: Background (participant name, professional background, and sequential `SME-N` ID) → Video Demo → Interview (Q2-Q6) → Category Validation Exercise → Hands-on Prototype → SUS → Done.

The **SUS-only route** collects only participant name and familiarity with Dart/Kotlin before receiving the same sequential `SME-N` ID, then follows: Video Demo → Hands-on Prototype → SUS → Done. It skips the full SME professional profile, Interview, and the Category Validation Exercise.

The Category Validation Exercise is the centerpiece of the full route. Six real findings from a code-review AI tool are shown one at a time, code on the left and the question on the right (side-by-side above 600px, stacked below it). The participant must guess which of 4 categories (Code Quality / Bugs / Optimization / Readability) a finding belongs to **before** the AI's actual answer is revealed, then state agreement. The Hands-on Prototype is a researcher-supervised `MySejahteraAlpha.dart` task in the embedded Glance web build: run a review, open feedback, apply one suggested fix. SUS is the standard 10-item System Usability Scale and stays locked until all four hands-on milestones are saved.

All responses are written to a Supabase (Postgres) database via its auto-generated REST/RPC API.

## Why this exists instead of a Google Form or paper worksheet

The whole point of the Category Validation Exercise is measuring whether the participant's own independent judgment matches the AI's category assignment. If the AI's answer is visible before they guess — even by accident, even just by them scrolling ahead — the measurement is contaminated (anchoring bias). Google Forms cannot enforce "no looking ahead" (nothing stops back-navigation between sections). A paper worksheet requires the researcher to physically hide the answer sheet section by section, which is error-prone and doesn't scale to multiple researchers or self-administered sessions.

**This is the one rule that must never be violated in any future change:** the AI-assigned category for a finding must not exist anywhere reachable by the browser (page source, JS variables, network responses) until *after* the participant's guess for that specific finding has already been recorded server-side. See "The blind-reveal boundary" below for exactly how this is enforced.

---

## File map

```
index.html            Page structure — Cover + Consent + route selector + study sections, toggled via .active class
style.css              All styling. No CSS framework, no build step.
app.js                 All client-side logic: state machine, validation, rendering, backend calls
researcher.html        Private researcher results page, linked only from the study footer.
researcher.js          Password sign-in, authenticated dashboard rendering, aggregate chart generation, and CSV/PNG export logic.
config.js              Two lines: SUPABASE_URL, SUPABASE_ANON_KEY. Contains the deployed public project settings.
supabase/migration.sql The full backend schema, for a fresh Supabase project.
supabase/003_add_hands_on_task.sql Incremental live-project migration for the Hands-on Prototype step.
supabase/004_add_participant_consent.sql Incremental live-project migration for the recorded consent gate.
supabase/005_add_study_path.sql Incremental live-project migration for full-SME and SUS-only study paths.
supabase/006_add_researcher_dashboard.sql Incremental migration for authenticated, allowlisted researcher results.
supabase/007_add_shared_background_and_identity.sql Incremental migration for shared background records and a separately protected participant-name table.
supabase/008_make_sus_background_route_specific.sql Follow-up for installations that ran the earlier 007, replacing the SUS-only professional profile with language familiarity.
README.md              End-user (non-technical) deploy instructions: create a Supabase project, run the migration, host the frontend.
smoke_test.mjs         Playwright test that drives the REAL app.js against a stubbed backend. Run this after any change.
preview_shots.mjs      Older/legacy script that fakes the UI by injecting DOM directly (bypasses app.js entirely).
                        Superseded by smoke_test.mjs's screenshots. Keep for reference, don't treat its output as authoritative.
gas/Code.gs            The PRIOR backend (Google Apps Script). No longer deployed or referenced by app.js — kept purely
                        as historical/reference material for how the same contract was implemented before the Supabase
                        migration. Do not resurrect this without a deliberate decision; app.js talks to Supabase now.
```

There is no build step, no package.json-driven bundler, no framework. Everything is vanilla HTML/CSS/JS on purpose, so it can be dropped straight onto GitHub Pages (or any static host) with zero tooling — including the Supabase calls, which go through plain `fetch()` against Supabase's REST/RPC endpoints, not the `@supabase/supabase-js` SDK (that would require either a bundler or a third-party `<script>` tag, both forbidden here). **Do not introduce a bundler, npm dependency, or framework unless the user explicitly asks for one** — that would break the "just push these 4 files to a repo" deploy story described in README.md. (`package.json` exists only to pin `playwright` as a dev/test dependency for `smoke_test.mjs` — it is not part of the deployed site.)

---

## The blind-reveal boundary (read this before editing anything in app.js or supabase/migration.sql)

`supabase/migration.sql` creates a `findings` table (num, line, title, category for all 6 planted issues). This is the **only** place the category exists before a guess is submitted — and it's locked down with Row Level Security (zero policies, default-deny), so the public `anon` role cannot read it directly under any circumstance, only through the one function that's allowed to.

`app.js` has a parallel `FINDINGS_PUBLIC` array — same findings, **same line numbers and titles, but no `category` field**. This is intentional duplication, not a bug: line/title are safe to ship to the client (the researcher already reads these aloud per the exercise's own script; `renderFinding()` shows the title and line up front), the category is not.

The flow: client calls the `submit_guess` RPC (`POST {SUPABASE_URL}/rest/v1/rpc/submit_guess`) with `{p_id, p_finding_num, p_guess}` → the Postgres function (running `SECURITY DEFINER`, so it can read the locked-down `findings` table) looks up the real category, **inserts** the guess+category into `category_validation`, and **only then** returns `{title, line, category, explanation}` in its response — the write happens before the reveal, in the same transaction. `revealFinding()` in app.js is the only place the category (or the explanation) ever touches the DOM, and `state.revealedFindings` (used to keep already-revealed findings colored in the source panel as the participant progresses) is only ever populated inside `revealFinding()`, never earlier.

**`explanation` (the "why" the app assigned that category) gets the exact same gate as `category` itself, not a looser one.** It's a column on the same locked-down `findings` table, only ever returned by `submit_guess`, same as category — never in `FINDINGS_PUBLIC`. This matters because the explanation text alone (e.g. "the merchant key is embedded as a string literal") gives the category away just as effectively as printing the label would, so treating it as "just descriptive text, safe to show early" would quietly reopen the boundary. If you ever add more per-finding fields, ask the same question before deciding where they're safe to live: does this field, on its own, let someone infer the category before their guess? If yes, it belongs in `findings`/`submit_guess`'s response, not `FINDINGS_PUBLIC`.

**If you ever change the findings** (different demo file, different planted issues, etc.), you must update both the `insert into findings` block in `supabase/migration.sql` (with category) and `FINDINGS_PUBLIC` in `app.js` (without category) — and re-run the changed `insert`/table setup in the Supabase SQL Editor, plus double check nothing in app.js accidentally imports or hardcodes a category value anywhere else.

**Threat model note, not a gap introduced by any particular backend choice:** this boundary protects against a category being reachable through *normal use of the page*. It does not stop someone who deliberately crafts a raw request straight to `submit_guess` for a finding they haven't reached in the UI yet — the anon key genuinely has `EXECUTE` on that function, and the function's whole job is to return the category. This was equally true of the prior Apps Script `/exec` endpoint. Closing it would require a per-participant, server-issued single-use token — a larger design change, not something either backend does today.

---

## Backend: Supabase (Postgres + PostgREST), not Apps Script

The backend used to be a Google Apps Script Web App bound to a Google Sheet (`gas/Code.gs`, still in this repo for reference). It was migrated to Supabase after the Apps Script Web App proved unreliable in practice — live testing found it intermittently returning Google Drive's "unable to open the file" error (confirmed via repeated probing: as low as ~20% success rate at times), which turned out to be inherent to Apps Script's free-tier Web App serving layer under light/sporadic traffic, not fixable from any setting. If you're asked to debug backend flakiness again, don't assume it's a Supabase-side issue by default — Supabase's REST/RPC layer is a real hosted Postgres API, not a lightweight scripting sandbox, so the failure mode is structurally different. But do still sanity-check the basics (project not paused — Supabase's free tier pauses after ~1 week of inactivity; RLS/grants intact; anon key correct) before assuming it's "just flaky."

### RLS + RPC design (why this isn't a plain CRUD API)

Every table (`intake`, `participant_identity`, `consent`, `interview`, `category_validation`, `hands_on_task`, `sus`, `findings`, `researcher_access`) has Row Level Security enabled with **zero** policies, default-deny for the public API. Eight `SECURITY DEFINER` functions are `REVOKE`d from `PUBLIC` and explicitly `GRANT`ed to `anon`: `assign_id`, `assign_sus_only_id`, `save_consent`, `save_interview`, `submit_guess`, `submit_agreement`, `save_hands_on_milestone`, `save_sus`. The ninth, `researcher_dashboard`, is granted only to `authenticated`; it also checks the signed JWT email against the locked-down `researcher_access` allowlist before it returns any data. `participant_identity` is never directly readable, and is joined only inside that authenticated dashboard function. Each function pins `search_path = ''` and fully-qualifies every table reference (`public.*`) to prevent a caller shadowing an unqualified name. A server-side trigger also rejects interview, CVE, hands-on, and SUS writes unless an accepted `consent` row exists. **If you add a new RPC function, follow this exact pattern** (RLS-locked tables, `SECURITY DEFINER`, pinned `search_path`, explicit revoke-then-grant), don't add a new table without RLS, and don't grant `anon` direct table access as a shortcut. See `supabase/migration.sql` for the full commented implementation.

### Backend contract (RPC functions)

Participant requests use `POST {SUPABASE_URL}/rest/v1/rpc/<function_name>`, headers `apikey` and `Authorization: Bearer <anon key>`, and a JSON body of `p_`-prefixed parameters. Their responses are `{"ok": true, ...}` or `{"ok": false, "error": "..."}`. `app.js`'s `callBackend()` translates the app's existing camelCase action/payload shape into this via `ACTION_MAP`. The researcher request is separate: `researcher.js` normally gets a signed Supabase Auth session through password sign-in, then calls `researcher_dashboard` with that session's Bearer token. A one-time email setup link remains available only to establish or replace the password.

| Action (app.js) | RPC function | Params | Response fields | Effect |
|---|---|---|---|---|
| `assignId` | `assign_id` | `p_years_experience`, `p_platforms`, `p_role`, `p_participant_name` | `id` (e.g. `"SME-3"`) | Inserts shared background into `intake` and the name into separate `participant_identity`; `id` is a generated column off an identity PK, atomic/race-safe under concurrent submissions |
| `assignSusOnlyId` | `assign_sus_only_id` | `p_participant_name`, `p_language_familiarity` | `id` (e.g. `"SME-4"`) | Inserts only the SUS participant name and Dart/Kotlin familiarity with `study_path = 'sus_only'`; no browser code, professional profile, Firebase identity, or application data is sent |
| `saveConsent` | `save_consent` | `p_id`, `p_accepted`, `p_consent_version` | — | Upserts an explicit accepted consent record, preserving its first timestamp |
| `saveInterview` | `save_interview` | `p_id`, `p_q2`..`p_q6` | — | Upserts a row into `interview` (one row per participant) |
| `submitGuess` | `submit_guess` | `p_id`, `p_finding_num` (1-6), `p_guess` | `title`, `line`, `category`, `explanation` — **the reveal** | Upserts a row into `category_validation` (agreement columns blank) |
| `submitAgreement` | `submit_agreement` | `p_id`, `p_finding_num`, `p_agreement`, `p_correct_category`, `p_could_also_be` | — | Finds the matching `category_validation` row (by `participant_id`+`finding_num`) and fills in the agreement columns; errors if no matching guess row exists yet |
| `saveHandsOnMilestone` | `save_hands_on_milestone` | `p_id`, `p_milestone`, `p_sample_id` | — | Idempotently records the first timestamp for one ordered task milestone on the fixed `mysejahtera-alpha-dart-v1` sample |
| `saveSUS` | `save_sus` | `p_id`, `p_scores` (array of 10 integers 1-5), optional `p_feedback_difficulty`, `p_feedback_improvement` | `susScore` (0-100, computed server-side from the ten fixed SUS items only) | Upserts a row into `sus`; feedback is capped at 1,000 characters and is not part of the score |
| Researcher results (`researcher.js`) | `researcher_dashboard` | none, authenticated Supabase Auth JWT required | Participant overview, category validation and interview data | Returns records only when the signed email is in `researcher_access` |

`participantName`, `yearsExperience`, `platforms`, `languageFamiliarity`, `agreement`, `correctCategory`, and `couldAlsoBe` are all sent as **plain strings**, even though the UI uses richer controls (slider, chips, radio buttons, toggle switch, ranked checkboxes) to produce them. The name is the one exception to the normal background record: assignment RPCs insert it into `participant_identity`, not `intake`. Full SME stores professional background fields; SUS-only stores only one constrained `language_familiarity` value. If you add a new field that needs its own column, add it to the relevant table in `supabase/migration.sql` **and** update the corresponding function's `insert`/`update` in the same change, or you get silent data loss. Unlike the old Sheet-based setup, adding a column to an existing table with data in it is a normal `alter table ... add column ...` in the SQL Editor, with no retrofit caveat.

### Schema (9 tables, `supabase/migration.sql`)

- **intake** — `participant_code` (generated, e.g. `SME-3`), timestamp, `study_path` (`full_sme` or `sus_only`), full-SME years of experience/platforms/role, and SUS-only `language_familiarity` (`Dart`, `Kotlin`, `Dart and Kotlin`, or `Neither`)
- **participant_identity** — participant ID, participant name, timestamp. It is RLS-locked and only the authenticated, allowlisted dashboard RPC may join it to an existing study record.
- **consent** — participant_id, accepted, consent_version, consented_at. It does not contain a name or study responses.
- **interview** — participant_id, timestamp, Q2..Q6 (one row per participant)
- **category_validation** — participant_id, finding_num, line, finding_title, guess (blind), ai_category (revealed), agreement, correct_category, could_also_be, guess_at, agreement_at — **one row per finding**, so 6 rows per participant, not 1
- **hands_on_task** — participant_id, fixed sample_id, opened_at, review_completed_at, feedback_opened_at, fix_applied_at. It deliberately contains no code, Firebase identity/token, email, or Glance history.
- **sus** — participant_id, timestamp, sus1..sus10, sus_score, optional feedback_difficulty, optional feedback_improvement. The two feedback fields are returned only through the authenticated researcher dashboard and SUS CSV, never to the public study page.
- **findings** — server-only, locked down by RLS; the 6 planted issues with their real categories and a one-paragraph "why" explanation, both gated identically (see "The blind-reveal boundary" above)
- **researcher_access** — server-only email allowlist. It contains the approved researcher email but no participant response.

Join on `participant_id` across tables (via the Table Editor's relationships, or a SQL join) for analysis. The Table Editor in the Supabase dashboard gives a spreadsheet-like grid per table with CSV export — the rough equivalent of "open the Sheet" for a non-technical researcher.

---

## Deployment status (as of this handoff)

**The baseline study is live.** Supabase project ref `ebolduegdfgvtzwopqgz` (`https://ebolduegdfgvtzwopqgz.supabase.co`) is deployed and verified end-to-end (RLS confirmed blocking direct table reads with error 42501; `assign_id`/`submit_guess`/`submit_agreement` round-tripped correctly against the real project). `config.js` has the real URL and anon key, committed. Frontend is on GitHub Pages: repo `https://github.com/syaheerdnl/GlanceSMEResponses` (public), live at `https://syaheerdnl.github.io/GlanceSMEResponses/`.

**Hands-on Prototype implementation and deployment (13 August 2026):** the seventh survey step has strict same-origin iframe/nonce message checks, four idempotent ordered milestone writes, and reload recovery. The parent passes only `studyNonce` in `prototype/?studyNonce=...`; it never sends the SME participant ID or survey answers to Glance. The child posts `{type: 'glance-study:milestone', nonce, milestone}` only. The dedicated Firebase Web app **Glance SME Prototype** is registered in Firebase project `glance-app-503806`; Anonymous Authentication is enabled and `syaheerdnl.github.io` is an authorised domain. The real, Firebase-authenticated production build is published under `prototype/` at `https://syaheerdnl.github.io/GlanceSMEResponses/prototype/`; GitHub Pages workflow run #15 completed successfully. Direct live verification loaded the fixed `MySejahteraAlpha.dart` task with zero browser-console errors, ran a real review, opened feedback, and applied the suggested fix. The production web build deliberately excludes all Google-sign-in code: the normal Android-only Google flow is conditionally isolated from the anonymous study build. The study build silently authenticates as a guest, skips only Login and Sign-up, and otherwise exposes the full Glance experience inside a desktop phone frame: Home, New Review, Paste, Upload, Scan, History, Projects, Profile, fixes, and About. New Review initially contains `MySejahteraAlpha.dart`, but a participant may replace it or review their own code. Their personal code is never sent to the SME Supabase database. The four study milestones, and therefore the SUS unlock, are recorded only when the unchanged prepared sample is reviewed, feedback is opened, and a suggested fix is applied. **Before a real participant session, execute `supabase/003_add_hands_on_task.sql`, `supabase/004_add_participant_consent.sql`, `supabase/005_add_study_path.sql`, and the current `supabase/007_add_shared_background_and_identity.sql` in Supabase SQL Editor**. If the earlier 007 was already executed, follow it with `supabase/008_make_sus_background_route_specific.sql`. Without these migrations, the parent cannot save milestones, consent, route records, protected names, or SUS language familiarity and should not be used for collection. Do not publish a `FORCE_MOCK_SERVICE=true` build. After collection, remove the deployed prototype or disable Anonymous Authentication to limit uninvited Gemini usage.

**Release readiness check (14 August 2026):** GitHub Pages was verified live after commit `d70e489`: the cover, protected researcher page, direct 58 MB demonstration video, and the Firebase-backed Flutter prototype all load with no browser-console errors. The visible consent label is `sme-web-consent-v4`, matching the value sent to `save_consent`, and the complete Playwright smoke test passed. A shared study device must use Glance Profile → **Start new guest session** between participants; Firebase anonymous login otherwise intentionally restores the current guest's review history on reload. Before collecting real data, confirm that migration 009 has run in Supabase, then remove the known `SME-1` verification row described below.

**Researcher setup redirect fix (14 August 2026):** Supabase GoTrue accepts a requested email-auth destination as the `redirect_to` query parameter on `/auth/v1/otp`, not as a JSON-body property. Sending it in the JSON body caused Supabase to ignore the researcher-page destination and fall back to the configured GitHub profile-root Site URL, which has no Pages site and returns 404. The setup request now sends the exact allowlisted `researcher.html` URL through the query parameter, and the smoke test asserts that contract. Previously sent links cannot be repaired; request a fresh setup link after this deploy.

**Pie-chart summary refinement (14 August 2026):** the researcher dashboard now renders Google-Forms-style donut charts for two valid part-to-whole summaries: the completed-SUS score-band distribution and the full-SME versus SUS-only route split. Their labels retain counts and percentages, and the aggregate Charts PNG export now renders matching canvas donuts. Hands-on milestones and category-match rates intentionally remain bar charts because they are cumulative/comparative measures, not mutually exclusive parts of one whole. The full smoke test and a visual dashboard screenshot passed.

**Chart stylesheet cache fix (14 August 2026):** `researcher.html` versions `style.css` with the same `20260814-pie-charts` cache key as `researcher.js`. This prevents a browser from pairing the new donut-chart markup with a previously cached stylesheet, which otherwise displays the charts as unstyled coloured rectangles. The smoke test now verifies the matching stylesheet URL.

**Recorded consent implementation (13 August 2026):** local code adds a Consent page between Cover and Background. The participant cannot progress without ticking the explicit agreement, and `save_consent` records only `participant_id`, `accepted=true`, `sme-web-consent-v4`, and the first consent timestamp. Consent v4 explains that names are held separately, SUS-only participants record Dart/Kotlin familiarity rather than a professional profile, and the two written post-SUS prompts are optional study feedback. Migration 009 upgrades the earlier consent versions while still accepting v1/v2/v3 for a disrupted pre-change session. The consent migration also installs database triggers that reject all interview, CVE, hands-on, and SUS writes without recorded consent. The wording is a practical web form based on the current study scope, not a substitute for any faculty ethics-approved participant-information sheet; check it with the supervisor before live collection.

**SUS interpretation and optional feedback (13 August 2026):** SUS remains the original ten-item, five-response instrument; neither its wording nor scoring formula has changed. The two optional prompts after it ask what was difficult/confusing and what single change would improve Glance. They are deliberately excluded from `localStorage`, capped at 1,000 characters on the server, and visible only in the authenticated researcher dashboard's SUS CSV. Consent v4 now describes them. The participant thank-you page confirms recording without showing a numerical score. For analysis, 68 is an aggregate reference point, never a participant pass/fail requirement; lower scores are evidence for a future design iteration. Existing live projects must run `supabase/009_add_optional_sus_feedback.sql` after their current migration sequence. Its two-argument `save_sus` compatibility wrapper lets a browser tab with cached pre-009 JavaScript finish without optional feedback.

**Full-SME / SUS-only route selection (13 August 2026, local until deployed):** after consent, browser code `0811` selects the full SME route. The no-code path selects the SUS-only route. Both routes record the participant name in `participant_identity`, then record consent and open Demo. Full SME adds the professional profile in `intake`; SUS-only adds only one selected Dart/Kotlin familiarity value in `intake`. The browser code itself is never stored. The SUS-only participant follows Background → Demo → Hands-on → SUS → Done, and the progress stepper hides only the SME-only Interview and Category Check stages. The code is deliberately not a security or authentication mechanism, so researcher supervision remains required. Run `supabase/005_add_study_path.sql` **after** migrations 003 and 004, then run the current `supabase/007_add_shared_background_and_identity.sql` before publishing this frontend. If the older 007 was applied already, run 008 afterwards. Existing rows are backfilled as `full_sme` and have no participant name unless separately added.

**Private researcher results (14 August 2026, local until deployed):** `researcher.html` is a separate, non-participant dashboard reachable from the study footer. Routine access is password sign-in for the single approved address, `muhammadsyaheerdaniel@gmail.com`, so it does not consume magic-link email quota. A one-time setup link remains only for establishing the first password: it opens the protected dashboard, where the authenticated researcher sets a 12-character-or-longer password. `researcher.js` stores only the short-lived access token in `sessionStorage` and calls `researcher_dashboard`. The one-time callback is explicitly fixed to `https://syaheerdnl.github.io/GlanceSMEResponses/researcher.html`, so opening a local `file://` copy for testing cannot make Supabase fall back to an invalid local URL. That RPC is granted to `authenticated`, not `anon`, then checks the signed JWT email against the locked-down `researcher_access` table. It returns participant overview data, including names only inside the protected dashboard and participant export, plus CSV export data filterable as All, Full SME, or SUS-only. The Participants CSV includes SUS-only `language_familiarity`. The visual summary applies the same route filter to SUS-score distribution, route split, hands-on milestones, and blind category-match charts. It exports a Summary CSV and a PNG containing aggregate values only; neither contains names, interview notes, or SUS free-text feedback. It never sends results to a browser PIN or exposes direct table reads. Before deploying this frontend, run the current 007 after 003, 004 and 005; if older 007 was already applied, run 008 afterwards. Then configure Supabase Authentication URL Configuration with Site URL `https://syaheerdnl.github.io/GlanceSMEResponses/` and Redirect URL `https://syaheerdnl.github.io/GlanceSMEResponses/researcher.html`; keep the Email provider enabled for password sign-in.

**Known cleanup item:** a verification pass during setup created a real test row — `SME-1` in `intake` (role "Verification Test") plus one `category_validation` row for finding 1. Delete these from the Supabase Table Editor before the first real participant, so IDs start clean.

**Resolved deployment check (13 August 2026):** the earlier 404 did not stem from a failed GitHub Pages build. GitHub's Actions tab shows every `pages-build-deployment` run through run #8 as successful; the latest build, deployment, and report jobs all completed successfully, and its published artifact is 55 MB. Repository Settings > Pages confirms the site deploys from `main` at `/ (root)`. Direct verification of `https://syaheerdnl.github.io/GlanceSMEResponses/glance-overview.mp4` returns **200 OK** as `video/mp4`, with byte-range support and a 58,278,041-byte content length. The live page now contains the six-step flow, including Demo, and its video element points to `glance-overview.mp4`. No Git LFS migration, re-encoding, or external host is required. The original 404 was most likely observed while the Pages/CDN rollout or cache had not yet caught up, rather than a persistent build failure. The only current Actions annotation is GitHub's non-blocking Node.js 20 deprecation warning for its internal Pages actions.

**Nine Supabase SQL files matter for the live project:** `supabase/migration.sql` (full schema, for a fresh install), `supabase/002_add_explanation.sql` (incremental, adds the `explanation` column and updates `submit_guess`), `supabase/003_add_hands_on_task.sql` (incremental, creates the milestone-only hands-on record and RPC), `supabase/004_add_participant_consent.sql` (incremental, creates recorded consent and the response-protection triggers), `supabase/005_add_study_path.sql` (incremental, marks full-SME/SUS-only routes), `supabase/006_add_researcher_dashboard.sql` (the original dashboard migration), `supabase/007_add_shared_background_and_identity.sql` (protected names, current full-SME/SUS-only background fields, consent v3, and enhanced dashboard RPC), `supabase/008_make_sus_background_route_specific.sql` (upgrade for the original 007 implementation), and `supabase/009_add_optional_sus_feedback.sql` (optional post-SUS feedback, consent v4, and protected SUS CSV export). For an existing project run 003, 004, 005, then the current 007. If original 007 was already run, execute 008 afterwards. Run 009 last. If 006 was already run, leave it in place.

**Prior deployment (superseded):** a Google Apps Script backend was previously deployed and live-tested; it proved unreliable (intermittent "unable to open the file" errors from Google, as low as ~20% success in repeated probing) and was replaced by the Supabase migration above. `gas/Code.gs` remains in the repo as reference but is not wired to anything.

---

## How to verify changes (do this before calling anything done)

`smoke_test.mjs` is a Playwright script that:
1. Intercepts `config.js` to inject fake `SUPABASE_URL`/`SUPABASE_ANON_KEY` values
2. Intercepts POST requests to `**/rest/v1/rpc/*` and returns realistic stubbed responses matching the real RPC contract above (dispatching on the function name in the URL path, not a `body.action` field — different from how the old Apps Script stub worked)
3. Drives the actual `app.js` through the entire flow via real DOM interactions (clicks, fills) — not mocked/injected content
4. Asserts on real behavior: consent blocking and its exact record payload, both route selections and their anonymous IDs, SME-only stage skipping for SUS-only participants, validation blocking (empty guess, incomplete SUS), the reveal only showing after guess submission, the stepper updating, the code line highlighting matching the current finding, resume-in-progress across reloads (including the guess-submitted-but-not-yet-agreed case, verifying no duplicate `submit_guess` call), strict iframe source/origin/nonce validation, idempotent hands-on milestone saving, SUS locking until all four actions, researcher password sign-in and one-time setup verification, route-filtered dashboard results, aggregate chart rendering, CSV/PNG exports, and zero console/page errors
5. Screenshots each of the major states to `preview/real-*.png`

Run it with:
```bash
cd <project folder>
npm install playwright   # if not already available
node smoke_test.mjs
```
(If `node` can't resolve `playwright` via a normal `npm install` in your environment, you may need `PLAYWRIGHT_BROWSERS_PATH`/`PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD` env vars or a local `node_modules/playwright` symlink to a global install — this was needed in one sandboxed environment already, may not apply to yours. `npx playwright install chromium` is needed the first time to fetch a real browser binary.)

**Known non-bug:** `.section.active` and `.reveal-box` have a ~0.2-0.25s CSS fade-in animation. If you screenshot immediately after a state change without waiting, you'll capture a washed-out, partially-transparent frame that looks broken but isn't — it's mid-animation. Always `await page.waitForTimeout(300-350)` after a section/reveal transition before screenshotting, or check `smoke_test.mjs` for the pattern already in place. This bit us once already during this project; don't rediscover it as a "regression."

After running the smoke test, actually **look at the screenshots** (don't just trust the assertions) — several real layout bugs during this project were only caught visually (a code block clipping long lines instead of wrapping, a stale post-reveal instruction line, for instance), not by the assertions.

**The sandboxed browser preview tool cannot be trusted for this project's local `file://` pages** — it renders them as an unstyled "static snapshot" with no real CSS/JS execution, in at least one environment this project was worked in. Use Playwright (via `smoke_test.mjs` or a throwaway script) for any real visual/responsive verification instead.

---

## UX/design conventions already established (match these in any new work)

- **No raw free-text where a structured control does the job better.** This was an explicit ask from the user partway through the project — see the years-of-experience slider, platform chips, agreement toggle, and ranked could-also-be checkboxes for the pattern to follow. If you're adding a new input, ask whether a slider/chips/toggle/pills would serve better than a text box before defaulting to `<input type="text">`.
- **Never send a different payload shape to the backend just because the input widget changed.** Convert structured UI state back into the same string format the backend already expects (see "Backend contract" above). This keeps the RPC functions stable across frontend iterations so the user doesn't have to touch `supabase/migration.sql` for every UI tweak.
- **No em dashes in any UI copy or generated text.** This is a standing convention across the user's whole thesis project (enforced elsewhere via an explicit "dash-check" step in their document-editing workflow). Use a comma, semicolon, or restructure the sentence instead.
- **Progress stepper at the top must stay in sync** with whatever section is actually showing — `updateStepper()` in app.js is the single place this happens, driven by `showSection()` and either `FULL_SME_STEPS` or `SUS_ONLY_STEPS`. If you add a new section, update the relevant route arrays and add its `<div class="step" data-step="...">` entry in `index.html`.
- **Exception for Cover, Consent, and the route selector:** these screens appear before the response-collection flow, so they deliberately have no stepper entry; `showSection()` hides the stepper while they are visible. Cover stores no data, Consent writes only after an anonymous ID exists, and the selector stores no code. Do not place participant-specific or CVE-reveal information on these screens.
- Visual style: a plain, institutional research-service interface adapted from the information hierarchy, start-page pattern, and form conventions of the GOV.UK Design System. This is an implementation reference only: do not use GOV.UK names, branding, assets, or copy. Use a system Arial/Helvetica sans-serif stack, white space, restrained green actions, black text, and thin grey separators. Avoid gradients, soft shadows, card-heavy composition, decorative type, and excessive rounding. Keep one clear primary action per page. The SUS section is deliberately a 10-row alternating-white/light-grey grid with five large custom radio tap targets per row; native inputs remain available for semantics and keyboard use. Keep minimal iconography (none currently; text and colour do the work) rather than introducing a competing visual language.
- **Category key:** use the fixed Glance taxonomy colours consistently in the CVE reference card and category-choice controls: Code Quality `#BF4F4B`, Bugs `#A8791E`, Optimization `#3C7F63`, and Readability `#6E63A6`. These colours only identify the four possible category names, so they are safe before a guess. A colour attached to a specific source line, finding dot, or application answer remains forbidden until `submit_guess` has saved that finding's guess and `revealFinding()` runs.
- **Exception, scoped to the CVE section only:** the source panel and finding card there deliberately adopt the real Glance mobile app's own visual language (quiet neutral panels, category dot/color system, and monospace source lines) instead of the general editorial identity — see the `.cve-glance`-scoped CSS block in `style.css` and its comments. This was a deliberate, explicitly-requested exception, not drift; don't "fix" it back to match the rest of the page, and don't let it leak into Intake/Interview/SUS.
- **CVE layout is side-by-side above 600px** (code on the left in `.col-code`, the question in a sticky `.col-finding` on the right), stacking vertically below that breakpoint. The source panel auto-scrolls the current finding's line into view (`scrollToCurrentLine()` in app.js) rather than leaving the participant to hunt for the highlight manually.
- **Back navigation exists for Background, Demo, and Interview only. CVE, Hands-on Prototype, and SUS deliberately have no Back button, and nothing has a way to navigate back into CVE.** On both routes, Demo's Back returns to Background without minting another ID. This is not an oversight: the whole reason this is a custom page instead of a Google Form is that Forms can't stop back-navigation, which would let a participant see CVE answers out of order. Reviewing/rewatching Demo or editing Interview doesn't touch that boundary, so it's fine to allow. The hands-on step is deliberately forward-only so SUS cannot be used before the recorded task actions. `saveInterview` is a safe upsert (`on conflict (participant_id) do update`), so re-submitting Interview after going back and forth is harmless. `assignId` is NOT an upsert, it always inserts a fresh row, so `initIntake()`'s Continue handler explicitly guards against re-calling it once `state.participantId` is already set. If you ever add another non-idempotent action reachable via Back, it needs the same guard.
- **`glance-overview.mp4`** (in the project root, ~56MB) is a real screen recording of the Glance app, shown via a plain `<video>` element on the new Demo section between Intake and Interview — no third-party video host or embed script, consistent with the no-external-dependency rule. It's committed directly to the repo; GitHub Pages serves it with range-request support so it streams rather than needing a full download first.

---

## Things NOT to do without checking with the user first

- Don't add localStorage/sessionStorage without confirming it's wanted — already added (resume-in-progress), see above; this note is about *additional* client-side storage beyond that, if ever proposed.
- Don't change the participant ID scheme (`SME-N`, sequential, assigned server-side) — it's referenced by this exact convention in the thesis's own supporting documents (SME Interview Draft Questions, Post-Viva materials). The Supabase migration preserves this exactly (see `intake.participant_code`).
- Don't add analytics, external fonts, or third-party script tags — this is a small academic-research instrument, not a product; minimize external dependencies and privacy surface area. The temporary Google Fonts exception granted on 13 August 2026 was removed the same day when the interface moved to its system-font service pattern. This includes not adding the `@supabase/supabase-js` SDK — plain `fetch()` against Supabase's REST/RPC endpoints is the deliberate choice, see "File map" above.
- Don't change the 4 feedback categories (Code Quality / Bugs / Optimization / Readability) or their definitions — these are fixed by the thesis's own taxonomy, not this tool's to redefine.
- Don't grant `anon` direct table access on any Supabase table, or add a new table without Row Level Security enabled, as a shortcut around writing a proper `SECURITY DEFINER` function — this is exactly the hole the current backend design closes; see "RLS + RPC design" above.
- Don't replace the researcher password sign-in with a browser PIN or put a researcher password/access token in source code. Results must remain protected by the server-side signed-email allowlist.

---

## Reasonable next steps, if asked for enhancement ideas (not a to-do list, just context)

- A researcher-only "review before submit" screen at the end of each section, in case they want to correct a typo before it's written to the database (currently every section submit is immediate/final).
- Exporting a printable/PDF summary of one participant's full session from Supabase, for attaching to thesis appendices.
- Accessibility pass (keyboard navigation through the custom radio-pill/chip controls, ARIA roles) — the current controls are clickable divs with a native `<input>` inside for semantics, but haven't been tested with a screen reader.
- Closing the "deliberate direct RPC call" gap noted in "The blind-reveal boundary" above, via a per-participant server-issued single-use token — a real design change, not a quick fix, only worth it if the threat model genuinely calls for it.

None of these were requested yet. Confirm scope with the user before building any of them.
