# Dev/test tooling only — per README.md Part 2, only index.html, style.css,
# app.js and config.js need to go into the deploy repo.
node_modules/
package-lock.json

# smoke_test.mjs screenshot output — regenerate locally with `node smoke_test.mjs`.
preview/
